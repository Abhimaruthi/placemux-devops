services:
 backend:
  build: ./backend
  container_name: vayuguard-backend
  ports:
    - "5000:5000"

 frontend:
  build: ./frontend
  container_name: vayuguard-frontend
  ports:
    - "5173:5173"
  depends_on:
    - backend
